const prompt = require('prompt-sync')();

    let num1 = Number(prompt('digite o primeiro numero'))
    if ( num1 % 2 == 0){
        console.log(`${num1} vai ser par`)
    }else {
        console.log(`${num1} vai ser impar`)
    }





    